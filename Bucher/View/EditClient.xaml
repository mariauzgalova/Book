<Window x:Class="Bucher.View.EditClient"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bucher.View"
        mc:Ignorable="d"
        Title="Редактировать клиента" Height="450" Width="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Margin="10, 0, 10, 10">

            <Label Content="Клиент" />
            <ComboBox ItemsSource="{Binding Clients}" SelectedItem="{Binding SelectedClient}"/>

            <Label Content="Имя" />
            <TextBox Text="{Binding Name}" />

            <Label Content="Фамилия" />
            <TextBox Text="{Binding Lastname}" />

            <Label Content="Отчество" />
            <TextBox Text="{Binding Surname}" />

            <Label Content="Телефон" />
            <TextBox Text="{Binding Telephone}" />

            <Label Content="Адрес" />
            <TextBox Text="{Binding Address}" />

        </StackPanel>

        <Button Grid.Row="1" Content="Редактировать" Command="{Binding EditClientCommand}" />

    </Grid>
</Window>
